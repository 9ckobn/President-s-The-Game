using UnityEngine;

namespace Data
{
    public class AttributeTextData
    {
        public string Text;
        public Color Color;

        public AttributeTextData(string text, Color color)
        {
            Text = text;
            Color = color;
        }
    }
}